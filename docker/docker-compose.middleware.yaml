version: '3'
services:
  # The postgres database.
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      # The password for the default postgres user.
      POSTGRES_PASSWORD: newsinbox123456
      # The name of the default postgres database.
      POSTGRES_DB: newsinbox
      # postgres data directory
      PGDATA: /var/lib/postgresql/data/pgdata
    volumes:
      - ./volumes/db/data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  rsshub:
    # two ways to enable puppeteer:
    # * comment out marked lines, then use this image instead: diygod/rsshub:chromium-bundled
    # * (consumes more disk space and memory) leave everything unchanged
    image: diygod/rsshub
    restart: always
    ports:
        - '1200:1200'
    environment:
        NODE_ENV: production
        CACHE_TYPE: redis
        REDIS_URL: 'redis://:newsinbox123456@redis:6379/0'
        PUPPETEER_WS_ENDPOINT: 'ws://browserless:4000'  # marked
    depends_on:
        - redis
        - browserless  # marked

  browserless:  # marked
      image: browserless/chrome  # marked
      restart: always  # marked
      ulimits:  # marked
        core:  # marked
          hard: 0  # marked
          soft: 0  # marked
      ports:
          - '4000:3000'

  # The redis cache.
  redis:
    image: redis:6-alpine
    restart: always
    volumes:
      # Mount the redis data directory to the container.
      - ./volumes/redis/data:/data
    # Set the redis password when startup redis server.
    command: redis-server --requirepass newsinbox123456
    ports:
      - "6379:6379"

  # # The Weaviate vector store.
  # weaviate:
  #   image: semitechnologies/weaviate:1.19.0
  #   restart: always
  #   volumes:
  #     # Mount the Weaviate data directory to the container.
  #     - ./volumes/weaviate:/var/lib/weaviate
  #   environment:
  #     # The Weaviate configurations
  #     # You can refer to the [Weaviate](https://weaviate.io/developers/weaviate/config-refs/env-vars) documentation for more information.
  #     QUERY_DEFAULTS_LIMIT: 25
  #     AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED: 'false'
  #     PERSISTENCE_DATA_PATH: '/var/lib/weaviate'
  #     DEFAULT_VECTORIZER_MODULE: 'none'
  #     CLUSTER_HOSTNAME: 'node1'
  #     AUTHENTICATION_APIKEY_ENABLED: 'true'
  #     AUTHENTICATION_APIKEY_ALLOWED_KEYS: 'WVF5YThaHlkYwhGUSmCRgsX3tD5ngdN8pkih'
  #     AUTHENTICATION_APIKEY_USERS: 'hello@newsinbox.ai'
  #     AUTHORIZATION_ADMINLIST_ENABLED: 'true'
  #     AUTHORIZATION_ADMINLIST_USERS: 'hello@newsinbox.ai'
  #   ports:
  #     - "8080:8080"

  # # The Sandbox
  # sandbox:
  #   image: langgenius/newsinbox-sandbox:0.1.0
  #   restart: always
  #   cap_add:
  #     # Why is sys_admin permission needed?
  #     # https://docs.newsinbox.ai/getting-started/install-self-hosted/install-faq#id-16.-why-is-sys_admin-permission-needed
  #     - SYS_ADMIN
  #   environment:
  #     # The newsinboxSandbox configurations
  #     API_KEY: newsinbox-sandbox
  #     GIN_MODE: 'release'
  #     WORKER_TIMEOUT: 15
  #   ports:
  #     - "8194:8194"

  # Qdrant vector store.
  # uncomment to use qdrant as vector store.
  # (if uncommented, you need to comment out the weaviate service above,
  # and set VECTOR_STORE to qdrant in the api & worker service.)
  # qdrant:
  #   image: qdrant/qdrant:1.7.3
  #   restart: always
  #   volumes:
  #     - ./volumes/qdrant:/qdrant/storage
  #   environment:
  #     QDRANT_API_KEY: 'newsinbox123456'
  #   ports:
  #     - "6333:6333"
  #     - "6334:6334"